[phases.setup]
nixPkgs = ["ffmpeg", "python3", "fontconfig"]

[phases.install]
cmds = [
  "ln -sf $(which python3) /usr/bin/python3 || true",
  "ln -sf $(which python3) /usr/local/bin/python3 || true",
  "npm install"
]

[start]
cmd = "node src/index.js"
