[phases.setup]
nixPkgs = ["ffmpeg", "yt-dlp", "fontconfig"]

[phases.install]
cmds = [
  "npm install",
  "echo '=== Build Check ===' && which yt-dlp && yt-dlp --version && which ffmpeg && echo '=================='"
]

[start]
cmd = "sh start.sh"
