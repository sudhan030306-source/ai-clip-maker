[phases.setup]
nixPkgs = ["ffmpeg", "yt-dlp", "fontconfig"]

[phases.install]
cmds = [
  "npm install",
  "echo 'yt-dlp path:' && which yt-dlp && yt-dlp --version"
]

[start]
cmd = "node src/index.js"
